import org.hibernate.SessionFactory;
import org.hibernate.cfg.AnnotationConfiguration;
import org.hibernate.classic.Session;

import java.util.List;

public class Main {

    public static void main(String[] args){
        System.out.println("Hello world!");

        /*SessionFactory sessionFactory = new AnnotationConfiguration().configure().buildSessionFactory();
        Session session = sessionFactory.openSession();

        Student Rick = new Student("Rick","Edison",3);
        Student Morty = new Student("Morty","Edison",3);

        StudentService studentService = new StudentService();

        //studentService.createStudent(session);

        //System.out.println(studentService.readStudentList(session));

        //System.out.println(studentService.readStudentById(session,1));

        studentService.runProgram(session);

        session.close();*/

        List<Vykdytojai>
    }
}